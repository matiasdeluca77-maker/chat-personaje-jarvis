export function renderChat(container) {
  container.innerHTML = `
    <section class="chat">
      <h1>Chat</h1>
      <p>Acá va a ir la interfaz de chat (la construimos en el próximo paso).</p>
    </section>
  `;
}